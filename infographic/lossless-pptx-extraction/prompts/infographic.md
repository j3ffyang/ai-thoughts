Create a professional infographic following these specifications:

## Image Specifications

- **Type**: Infographic
- **Layout**: structural-breakdown (cross-section / exploded system view)
- **Style**: technical-schematic (engineering blueprint)
- **Aspect Ratio**: 16:9
- **Language**: English

## CRITICAL: Text Accuracy

The image must render EXACTLY these strings, spelled correctly, no duplicates, no extra words:

- Title: `EXTRACTING POWERPOINT TO MARKDOWN WITHOUT LOSING A BYTE`
- Subtitle: `A local pipeline, proven by a five-point audit`
- `ALL DATA, NO SUMMARY`
- `CONTENT` / `extracted`
- `CHROME` / `excluded`
- `UNRENDERABLE` / `disclosed`
- `source/` / `extract_pptx.py` / `extracted/` / `audit_pptx.py`
- `pptx-extract`
- `THE AUDIT IS THE PRODUCT`
- `slide count` / `media explained` / `links resolve` / `text complete` / `exclusions reported`
- `no image can vanish without a named reason`
- `WHY IT STAYS LOCAL`
- `GitHub Actions` (shown crossed out / rejected)
- `local runner`
- `80 MB confidential binaries`
- `backup is the cost`
- `FIVE BUGS ONLY REVIEW CAUGHT`
- `1 broken links` / `2 volatile date` / `3 wrong signature` / `4 substring check` / `5 latent crash`
- `btw, i use arch`

Pitfalls to avoid:
- Do NOT double words or repeat a label twice.
- Keep `pptx` lowercase and hyphenate `python-pptx` exactly once if shown.
- `UNRENDERABLE` must be spelled correctly (not UNRENDERBLE / UNRENDREABLE).
- `audit` must not be garbled (not audlt / audlt).
- Prefer FEWER labels over shrinking text into illegibility. If space runs short, drop the least important label rather than misspelling any.

## Core Principles

- Follow the layout structure precisely for information architecture
- Apply the blueprint style consistently throughout
- Keep information concise, highlight keywords and core concepts
- Use ample whitespace for visual clarity; maintain clear visual hierarchy

## Layout Guidelines (structural-breakdown)

- One central subject: the extraction pipeline drawn as a cutaway/exploded system.
- Show the parts separated with labeled callout lines and dot/arrow endpoints.
- Central flow left-to-right: `source/` → `extract_pptx.py` → `extracted/`, with `audit_pptx.py` as a verification overlay checking the output, and a `pptx-extract` skill block attached to the system.
- Label boxes at callout endpoints; numbered parts where useful.
- Optional zoomed detail panel for the audit checklist.
- Title at top; brief descriptions in boxes; a small legend if numbered.

## Style Guidelines (technical-schematic — blueprint)

- Engineering precision, clean geometry, consistent stroke weights, clean vector shapes.
- Palette: deep blue background (#1E3A5F) with white/light-gray linework and grid; primary blue (#2563EB), teal and gray fills; amber (#F59E0B) accents for highlights and warnings; cyan callouts.
- Blueprint grid across the background; dimension lines and technical symbols/annotations.
- Typography: clean sans-serif, all-caps labels, technical stencil feel.
- Layout regions:
  - Top: title band and subtitle.
  - Left column: `ALL DATA, NO SUMMARY` and the three-class taxonomy (CONTENT / CHROME / UNRENDERABLE).
  - Center: the cutaway pipeline with callouts (`source/`, `extract_pptx.py`, `extracted/`, `audit_pptx.py`, `pptx-extract`).
  - Right column: `THE AUDIT IS THE PRODUCT` checklist (five rows, first four as solid check marks, the fifth as a lighter "reported" badge) with the callout `no image can vanish without a named reason`.
  - Lower band: `WHY IT STAYS LOCAL` fork — `GitHub Actions` crossed out on one side, `local runner` chosen on the other; small labels `80 MB confidential binaries` and `backup is the cost`.
  - Bottom strip: `FIVE BUGS ONLY REVIEW CAUGHT` as five small numbered defect cards.
  - Footer: `btw, i use arch` small, bottom-right.

---

Generate the infographic based on the content below:

## The pipeline (central subject)
A `.pptx` is a zip of XML parts; `python-pptx` parses them; the project adds the guarantees.
Flow: `source/` → `extract_pptx.py` → `extracted/` (Markdown + images). `audit_pptx.py` verifies the output against the source. A skill, `pptx-extract`, codifies the whole procedure and travels with the folder.

## Requirement
`ALL DATA, NO SUMMARY` — proving nothing was lost matters more than the conversion itself.

## Three classes
- CONTENT — extracted
- CHROME — excluded
- UNRENDERABLE — disclosed

## Audit (the product)
Five checks: slide count; media explained; links resolve; text complete; exclusions reported. The first four are hard gates; the fifth is reported. `no image can vanish without a named reason`.

## Bugs
Five defects that only review caught: broken links, volatile date, wrong signature, substring check, latent crash.

## Hosting decision
`GitHub Actions` rejected because CI only sees what git sees and sources are `80 MB confidential binaries`; a `local runner` is chosen instead; `backup is the cost`.

Text labels (in English):
EXTRACTING POWERPOINT TO MARKDOWN WITHOUT LOSING A BYTE · A local pipeline, proven by a five-point audit · ALL DATA, NO SUMMARY · CONTENT · extracted · CHROME · excluded · UNRENDERABLE · disclosed · source/ · extract_pptx.py · extracted/ · audit_pptx.py · pptx-extract · THE AUDIT IS THE PRODUCT · slide count · media explained · links resolve · text complete · exclusions reported · no image can vanish without a named reason · WHY IT STAYS LOCAL · GitHub Actions · local runner · 80 MB confidential binaries · backup is the cost · FIVE BUGS ONLY REVIEW CAUGHT · 1 broken links · 2 volatile date · 3 wrong signature · 4 substring check · 5 latent crash · btw, i use arch